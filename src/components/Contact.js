import React from "react";

const Contact = (props)=>
{
    return (
        <div className="container">
            <h4 className="center">Contact</h4>
            <p>
                Welcome to React Router Contact Page
            </p>
        </div>

    );
};

export default Contact;